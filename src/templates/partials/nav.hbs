<nav class="g-nav">
    <ul class="g-nav__list">
        {{#each content.navigations.site}}
        <li class="g-nav__listItem">
            <a href="{{this}}" class="g-nav__listItemLink">{{@key}}</a>
        </li>
        {{/each}}
    </ul>     
</nav>