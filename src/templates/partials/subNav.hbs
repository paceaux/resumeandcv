<nav class="nav nav--sub" aria-label="Navigate within this section">
    <ul class="nav__list">
            {{#each this}}
                <li class="nav__listItem">
                    <a class="nav__listItemLink" href="{{this}}">{{@key}}</a> 
                </li>
            {{/each}}
    </ul>
</nav>